[package]
name = "my_crate"
version = "0.1.0"
authors = ["Anton Österberg <anton@42x.io>"]
edition = "2024"

[dependencies]
clap = { version = "4", features = ["cargo", "color", "derive", "suggestions"] }
regex = "1"
log = "0.4"
env_logger = "0.10"
termcolor = "1.2"

[profile.release]
lto = true
